# Trained model
# NOTE: They need to be booked in the correct order! The index (0,1) is
# matched with the dataset (1,0) so that the testing runs not on the
# training dataset.
model:
    - fold0_keras_model.h5
    - fold1_keras_model.h5

# NOTE: Same as for the model files.
preprocessing:
    - fold0_keras_preprocessing.pickle
    - fold1_keras_preprocessing.pickle

# Event weight branch
weight_branch: training_weight
